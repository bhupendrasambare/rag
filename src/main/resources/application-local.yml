server:
    port: 8080

spring:
    security:
        user:
            name: admin
            password: admin
    servlet:
        multipart:
            enabled: true
            max-file-size: 100MB
            max-request-size: 100MB
    datasource:
        url: jdbc:postgresql://localhost:5432/rag_db
        username: rag_user
        password: password
    jpa:
        open-in-view: false
        hibernate:
            ddl-auto: validate
        properties:
            hibernate:
                format_sql: true
                highlight_sql: true
                jdbc:
                    time_zone: UTC
    flyway:
        enabled: true
        locations: classpath:db/migration
        baseline-on-migrate: true
        validate-on-migrate: true
    ai:
        ollama:
            base-url: http://localhost:11434
            chat:
                model: phi3:latest
            embedding:
                model: nomic-embed-text
            init:
                pull-model-strategy: never
        vectorstore:
            pgvector:
                initialize-schema: false
                dimensions: 768

application:
        jwt:
            secret: 7A72B82C2D5A58FBC6B1D3E0B85F4A7A52C5D7E2EAB36B7F4A6B5F8C9D2A7E3F
            access-token-expiration: 900000
            refresh-token-expiration: 604800000
            issuer: rag-app

management:
    endpoints:
        web:
            exposure:
                include: health,info

logging:
    level:
        org.springframework.ai: INFO
        org.flywaydb: DEBUG